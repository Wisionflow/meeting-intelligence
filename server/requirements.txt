# Server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# Transcription — cloud APIs (optional, used when keys provided)
httpx>=0.27.0

# Transcription — local (always available, no API keys needed)
faster-whisper>=1.0.0

# Analysis (Claude)
anthropic>=0.40.0

# Database
asyncpg>=0.29.0

# Config
python-dotenv>=1.0.0
